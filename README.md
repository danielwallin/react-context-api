A simple webapp made quickly where I make use of the React context Api as an alternative way of component composition and state handling with state managers such as Redux.

```js
npm install && npm run start
```
